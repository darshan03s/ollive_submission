{
  "name": "ollive",
  "version": "0.0.0",
  "packageManager": "pnpm@10.28.2",
  "devDependencies": {
    "@types/node": "^26.0.0",
    "husky": "^9.1.7",
    "lint-staged": "^17.0.7",
    "prettier": "3.8.4"
  },
  "scripts": {
    "prepare": "husky",
    "install:all": "pnpm install && pnpm --filter db install && pnpm --filter ingest install && pnpm --filter web install"
  }
}
